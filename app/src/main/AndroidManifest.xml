<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vova.matesha">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo1"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ZnoCard"></activity>
        <activity android:name=".ZnoHolder" />
        <activity android:name=".Card" />
        <activity android:name=".TaskActivity" />
        <activity android:name=".ChoiseAction">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChoseChapter" />
        <activity android:name=".HolderActivity" />
    </application>

</manifest>